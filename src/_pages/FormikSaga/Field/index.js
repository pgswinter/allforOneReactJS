{/* <Field 
  type="text" 
  name="email" 
  placeholder="Email Address" 
  className='textbox'
  /> 

  {
      formProps.touched.email &&
      formProps.errors.email &&
      <span className="error">{formProps.error.email}</span>
  }
  --- Instead of shorter code
  <ErrorMessage name="email" />
   */
}